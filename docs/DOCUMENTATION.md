# Полная документация проекта CompareDocx

## Оглавление

1. [Описание проекта](#описание-проекта)
2. [Установка и настройка](#установка-и-настройка)
3. [Архитектура проекта](#архитектура-проекта)
4. [Использование](#использование)
5. [Детальное описание модулей](#детальное-описание-модулей)
6. [Алгоритмы сравнения](#алгоритмы-сравнения)
7. [Формат результатов](#формат-результатов)
8. [Особенности и ограничения](#особенности-и-ограничения)
9. [Примеры использования](#примеры-использования)
10. [Расширение функционала](#расширение-функционала)

---

## Описание проекта

**CompareDocx** - это инструмент для сравнения двух DOCX документов с детальным анализом изменений. Программа выполняет сравнение по содержимому текста, игнорируя стили форматирования, что позволяет находить идентичные абзацы даже при разных стилях.

### Основные возможности

- ✅ **Сравнение по содержимому** - сравнение текстов независимо от стилей форматирования
- ✅ **Детальный анализ структуры** - разбиение на разделы, подразделы, главы и абзацы
- ✅ **Сравнение таблиц** - анализ изменений в ячейках с указанием строк и столбцов
- ✅ **Сравнение изображений** - отслеживание добавления, удаления и изменения изображений
- ✅ **Полные пути до элементов** - построение иерархии "Раздел > Подраздел > Пункт"
- ✅ **Номера страниц** - вычисление примерных номеров страниц для каждого элемента
- ✅ **Типы исправлений** - классификация изменений (форматирование, содержание, добавление, удаление)
- ✅ **Экспорт в Excel** - структурированный экспорт с цветовой индикацией
- ✅ **Поддержка кастомных стилей** - работа с любыми стилями, не только стандартными

---

## Установка и настройка

### Требования

- Python 3.7 или выше
- Зависимости из `requirements.txt`

### Установка

1. **Клонирование или скачивание проекта**

2. **Установка зависимостей:**
```bash
pip install -r requirements.txt
```

Зависимости:
- `python-docx==1.1.0` - работа с DOCX файлами
- `openpyxl==3.1.2` - работа с Excel файлами
- `Pillow==10.1.0` - работа с изображениями (для создания тестовых документов)

### Структура проекта

```
compareDocx/
├── docx_file.py              # Класс DocxFile - парсинг документов
├── compare.py                 # Класс Compare - сравнение документов
├── excel_export.py            # Класс ExcelExporter - экспорт в Excel
├── main.py                    # Точка входа в программу
├── requirements.txt           # Зависимости проекта
├── README.md                  # Краткая документация
├── docs/                      # Папка с документацией
│   ├── DOCUMENTATION.md       # Полная документация (этот файл)
│   ├── CUSTOM_STYLES_SUPPORT.md   # Документация по кастомным стилям
│   └── CONTENT_BASED_COMPARISON.md # Документация по сравнению по содержимому
├── tests/                      # Папка со скриптами для создания тестовых документов
│   ├── create_test_documents.py   # Скрипт создания базовых тестовых документов
│   ├── create_extended_test_documents.py  # Скрипт создания масштабных документов
│   └── create_additional_test_documents.py # Скрипт создания дополнительных документов
├── documents/                 # Папка с тестовыми документами
│   ├── test_document_1.docx
│   ├── test_document_2.docx
│   ├── extended_test_document_1.docx
│   └── ...
└── results/                   # Папка для результатов сравнения
    └── *.xlsx
```

---

## Архитектура проекта

### Модульная структура

Проект разделен на три основных модуля:

1. **docx_file.py** - Парсинг и анализ DOCX документов
2. **compare.py** - Логика сравнения документов
3. **excel_export.py** - Экспорт результатов в Excel

### Поток данных

```
DOCX файлы → DocxFile (парсинг) → Compare (сравнение) → ExcelExporter (экспорт) → Excel файл
```

### Основные классы

#### DocxFile
- **Назначение:** Парсинг DOCX документов
- **Основные методы:**
  - `_parse_document()` - основной метод парсинга
  - `_get_heading_level()` - определение уровня заголовка
  - `_parse_tables()` - парсинг таблиц
  - `_parse_images()` - парсинг изображений
  - `_build_full_path()` - построение полного пути

#### Compare
- **Назначение:** Сравнение двух документов
- **Основные методы:**
  - `_compare_documents()` - сравнение абзацев
  - `_normalize_text()` - нормализация текста
  - `_compare_tables()` - сравнение таблиц
  - `_compare_images()` - сравнение изображений
  - `_build_change_description()` - построение описания изменений

#### ExcelExporter
- **Назначение:** Экспорт результатов в Excel
- **Основные методы:**
  - `export_comparison()` - основной метод экспорта
  - `_create_comparison_sheet()` - создание листа сравнения
  - `_create_changes_only_sheet()` - создание листа только с изменениями
  - `_create_tables_sheet()` - создание листа таблиц
  - `_create_images_sheet()` - создание листа изображений

---

## Использование

### Командная строка

```bash
python main.py файл1.docx файл2.docx результат.xlsx
```

**Параметры:**
- `файл1.docx` - путь к первому (базовому) документу
- `файл2.docx` - путь ко второму (измененному) документу
- `результат.xlsx` - путь к выходному Excel файлу (опционально)

### Интерактивный режим

```bash
python main.py
```

Программа запросит пути к файлам интерактивно.

### Примеры

```bash
# Базовое сравнение
python main.py documents/test_document_1.docx documents/test_document_2.docx results/comparison.xlsx

# Сравнение масштабных документов
python main.py documents/extended_test_document_1.docx documents/extended_test_document_2.docx results/extended.xlsx
```

---

## Детальное описание модулей

### docx_file.py - Парсинг документов

#### Класс DocxFile

**Основные атрибуты:**
- `paragraphs` - список всех абзацев с метаданными
- `sections` - список разделов документа
- `chapters` - список глав документа
- `tables` - список таблиц
- `images` - список изображений
- `hierarchy_stack` - стек для построения иерархии

#### Методы парсинга

**`_parse_document()`**
- Парсит весь документ, извлекая абзацы, разделы, главы
- Вычисляет примерные номера страниц
- Строит полные пути до элементов
- Игнорирует подписи к рисункам/таблицам при построении иерархии

**`_get_heading_level()`**
- Определяет уровень заголовка (0-9)
- Поддерживает 5 уровней приоритета:
  1. Outline level стиля (самый надежный)
  2. Стандартные стили Word
  3. Паттерны в названиях стилей
  4. Анализ форматирования (размер шрифта, жирность)
  5. Нумерация в тексте

**`_parse_tables()`**
- Извлекает все таблицы из документа
- Сохраняет содержимое всех ячеек
- Создает текстовое представление для сравнения
- Вычисляет хеш для быстрого поиска идентичных таблиц

**`_parse_images()`**
- Извлекает все изображения через relationships
- Резервный метод через XML
- Вычисляет хеш изображений для сравнения
- Сохраняет метаданные (размер, индекс абзаца)

#### Методы получения данных

- `get_all_paragraphs()` - получить все абзацы
- `get_sections()` - получить все разделы
- `get_chapters()` - получить все главы
- `get_tables()` - получить все таблицы
- `get_images()` - получить все изображения
- `get_structure_info()` - получить информацию о структуре

---

### compare.py - Сравнение документов

#### Класс Compare

**Основные атрибуты:**
- `file1`, `file2` - объекты DocxFile для обоих документов
- `comparison_results` - результаты сравнения абзацев
- `table_changes` - результаты сравнения таблиц
- `image_changes` - результаты сравнения изображений

#### Алгоритм сравнения абзацев

**Шаг 1: Нормализация**
- Все тексты нормализуются (удаление лишних пробелов, переносов строк)

**Шаг 2: Создание отпечатков**
- Для каждого абзаца создается "отпечаток" (первые/последние слова, длина)

**Шаг 3: Сопоставление**
- Используется SequenceMatcher для поиска похожих последовательностей
- Дополнительный поиск по отпечаткам для идентичных абзацев в разных позициях

**Шаг 4: Анализ изменений**
- Для каждого абзаца определяется статус (идентичен/изменен/добавлен/удален)
- Вычисляется схожесть (0.0 - 1.0)
- Строится описание изменений

#### Методы сравнения

**`_normalize_text()`**
- Нормализует текст для сравнения по содержимому
- Удаляет различия в форматировании

**`_get_differences()`**
- Возвращает полные тексты предложений/абзацев с изменениями
- Формат: "Старый текст: '...'", "Новый текст: '...'"

**`_build_change_description()`**
- Строит описание в формате "'старое' изменено на 'новое'"
- Включает полный путь, страницу, конкретные изменения

**`_determine_change_type()`**
- Определяет тип исправления:
  - "Изменение форматирования"
  - "Добавление текста"
  - "Удаление текста"
  - "Изменение содержания"
  - и т.д.

#### Сравнение таблиц

**`_compare_tables()`**
- Сравнивает таблицы по хешу содержимого
- Находит идентичные, измененные, добавленные, удаленные таблицы
- Для измененных таблиц находит конкретные изменения в ячейках

**`_find_table_cell_changes()`**
- Находит изменения в конкретных ячейках
- Возвращает список изменений с указанием строки и столбца

**`_build_table_change_description()`**
- Строит описание изменений в таблице
- Формат: "Строка X, столбец Y: 'старое' изменено на 'новое'"

#### Сравнение изображений

**`_compare_images()`**
- Сравнивает изображения по хешу
- Находит идентичные, измененные, добавленные, удаленные изображения
- Извлекает названия изображений из предыдущих абзацев

---

### excel_export.py - Экспорт в Excel

#### Класс ExcelExporter

**Основные методы:**

**`export_comparison()`**
- Главный метод экспорта
- Создает все необходимые листы
- Применяет форматирование

**`_create_comparison_sheet()`**
- Создает лист "Сравнение" со всеми абзацами
- Столбцы: №, Статус, Тип исправления, Полный путь, Страница, Абзац №, Текст, Схожесть, Различия, Описание изменений
- Цветовая индикация: зеленый (идентичен), желтый (изменен), красный (добавлен/удален)

**`_create_changes_only_sheet()`**
- Создает лист "Только изменения"
- Фильтрует только измененные, добавленные и удаленные элементы
- Использует тот же формат, что и основной лист

**`_create_statistics_sheet()`**
- Создает лист "Статистика"
- Показывает общую статистику сравнения
- Включает информацию о таблицах и изображениях

**`_create_tables_sheet()`**
- Создает лист "Таблицы"
- Столбцы: №, Статус, Название таблицы 1, Название таблицы 2, Описание, Описание изменений
- Детальное описание изменений в ячейках

**`_create_images_sheet()`**
- Создает лист "Изображения"
- Столбцы: №, Статус, Название изображения 1, Название изображения 2, Описание, Описание изменений

---

## Алгоритмы сравнения

### Нормализация текста

Текст нормализуется в несколько этапов:

1. **Удаление множественных пробелов** → один пробел
2. **Нормализация переносов строк** → пробелы
3. **Удаление специальных символов** → обычные пробелы
4. **Удаление пробелов в начале/конце**

**Результат:** Тексты с одинаковым содержимым, но разным форматированием становятся идентичными.

### Поиск похожих абзацев

Используется комбинация методов:

1. **SequenceMatcher** - находит похожие последовательности по позиции
2. **Отпечатки текста** - быстрый поиск идентичных абзацев независимо от позиции
3. **Поиск лучшего совпадения** - для несовпадающих абзацев ищется наиболее похожий

### Определение схожести

Схожесть вычисляется через `difflib.SequenceMatcher`:
- `1.0` - тексты идентичны
- `0.8-0.99` - тексты очень похожи (небольшие изменения)
- `0.6-0.79` - тексты похожи (значительные изменения)
- `<0.6` - тексты различаются

### Классификация изменений

Типы исправлений определяются автоматически:

- **"Без изменений"** - схожесть = 1.0
- **"Изменение форматирования"** - тексты идентичны после нормализации
- **"Добавление текста"** - добавлены новые слова
- **"Удаление текста"** - удалены слова
- **"Изменение содержания"** - и добавление, и удаление
- **"Изменение порядка слов"** - те же слова, другой порядок

---

## Формат результатов

### Excel файл - структура листов

#### Лист "Сравнение"

Полное сравнение всех абзацев:

| Столбец | Описание |
|---------|----------|
| № | Порядковый номер |
| Статус | Идентичен/Изменен/Добавлен/Удален |
| Тип исправления | Тип изменения (форматирование, содержание и т.д.) |
| Полный путь (Файл 1) | Иерархия до абзаца в первом документе |
| Страница (Файл 1) | Примерная страница в первом документе |
| Абзац № (Файл 1) | Номер абзаца в первом документе |
| Текст (Файл 1) | Полный текст абзаца |
| Полный путь (Файл 2) | Иерархия до абзаца во втором документе |
| Страница (Файл 2) | Примерная страница во втором документе |
| Абзац № (Файл 2) | Номер абзаца во втором документе |
| Текст (Файл 2) | Полный текст абзаца |
| Схожесть (%) | Процент схожести (0-100%) |
| Различия | Полные тексты с изменениями |
| Описание изменений | Текстовое описание в формате "'старое' изменено на 'новое'" |

#### Лист "Только изменения"

Тот же формат, что и "Сравнение", но только для измененных элементов.

#### Лист "Статистика"

Общая статистика:
- Всего абзацев
- Идентичных (количество и процент)
- Измененных (количество и процент)
- Добавленных (количество и процент)
- Удаленных (количество и процент)
- Изменено таблиц
- Изменено изображений

#### Лист "Таблицы"

Изменения в таблицах:

| Столбец | Описание |
|---------|----------|
| № | Порядковый номер |
| Статус | Идентична/Изменена/Добавлена/Удалена |
| Название таблицы 1 | Название таблицы в первом документе |
| Название таблицы 2 | Название таблицы во втором документе |
| Описание | Краткое описание изменения |
| Описание изменений | Детальное описание: "Строка X, столбец Y: 'старое' изменено на 'новое'" |

#### Лист "Изображения"

Изменения в изображениях:

| Столбец | Описание |
|---------|----------|
| № | Порядковый номер |
| Статус | Идентично/Изменено/Добавлено/Удалено |
| Название изображения 1 | Название в первом документе |
| Название изображения 2 | Название во втором документе |
| Описание | Краткое описание изменения |
| Описание изменений | Детальное описание: "'старое' изменено на 'новое'" |

### Цветовая индикация

- 🟢 **Зеленый** - Идентичные элементы
- 🟡 **Желтый** - Измененные элементы
- 🔴 **Красный** - Добавленные/удаленные элементы

---

## Особенности и ограничения

### Особенности

1. **Сравнение по содержимому**
   - Тексты сравниваются независимо от стилей
   - Абзацы с одинаковым текстом, но разными стилями → идентичны

2. **Поддержка кастомных стилей**
   - Работает с любыми стилями, не только стандартными
   - Определяет уровни заголовков через outline level, паттерны, форматирование

3. **Детальный анализ**
   - Полные пути до элементов
   - Примерные номера страниц
   - Конкретные изменения в таблицах (строка, столбец)

4. **Умное сопоставление**
   - Находит идентичные абзацы даже в разных позициях
   - Использует отпечатки для быстрого поиска

### Ограничения

1. **Номера страниц**
   - Вычисляются приблизительно (2000 символов на страницу)
   - Могут отличаться от реальных номеров в Word

2. **Названия таблиц/изображений**
   - Ищутся в предыдущих абзацах (эвристика)
   - Могут быть не найдены, если название далеко от элемента

3. **Сложные таблицы**
   - Очень большие таблицы могут обрабатываться медленнее
   - Изменения в форматировании ячеек не отслеживаются (только содержимое)

4. **Изображения**
   - Сравнение только по хешу содержимого
   - Изменения в размере/качестве не отслеживаются

5. **Производительность**
   - Для очень больших документов (>1000 абзацев) может быть медленнее
   - Рекомендуется использовать для документов до 500-1000 абзацев

---

## Примеры использования

### Пример 1: Базовое сравнение

```bash
python main.py documents/test_document_1.docx documents/test_document_2.docx results/basic.xlsx
```

**Результат:**
- Excel файл с детальным сравнением
- Статистика изменений
- Изменения в таблицах и изображениях

### Пример 2: Сравнение масштабных документов

```bash
python main.py documents/extended_test_document_1.docx documents/extended_test_document_2.docx results/extended.xlsx
```

**Результат:**
- Полный анализ большого документа
- Множество изменений в таблицах и изображениях
- Детальные описания всех изменений

### Пример 3: Программное использование

```python
from compare import Compare
from excel_export import ExcelExporter

# Сравнение документов
comparator = Compare("doc1.docx", "doc2.docx")

# Получение результатов
results = comparator.get_comparison_results()
statistics = comparator.get_statistics()
table_changes = comparator.get_table_changes()
image_changes = comparator.get_image_changes()

# Экспорт в Excel
exporter = ExcelExporter("result.xlsx")
exporter.export_comparison(
    results, statistics, "doc1.docx", "doc2.docx",
    table_changes, image_changes
)
```

---

## Расширение функционала

### Добавление новых типов элементов

Для добавления поддержки новых типов элементов (например, списков, сносок):

1. Расширьте `DocxFile._parse_document()` для извлечения элементов
2. Добавьте методы сравнения в `Compare`
3. Добавьте экспорт в `ExcelExporter`

### Изменение алгоритма сравнения

Для изменения алгоритма сравнения:

1. Модифицируйте `Compare._normalize_text()` для другой нормализации
2. Измените `Compare._calculate_similarity()` для другого алгоритма схожести
3. Настройте пороги в `Compare._compare_documents()`

### Кастомизация экспорта

Для изменения формата экспорта:

1. Модифицируйте методы `ExcelExporter._create_*_sheet()`
2. Измените форматирование и цвета
3. Добавьте новые столбцы или листы

---

## Часто задаваемые вопросы

### Q: Почему некоторые абзацы не распознаются как идентичные?

**A:** Проверьте:
- Нет ли различий в тексте (даже небольших)
- Правильно ли работает нормализация
- Попробуйте снизить порог схожести в коде

### Q: Как улучшить определение названий таблиц?

**A:** Улучшите функцию `get_table_name()` в `compare.py`:
- Расширьте диапазон поиска
- Добавьте больше паттернов для распознавания

### Q: Можно ли сравнивать документы с разными языками?

**A:** Да, программа работает с любыми текстами. Нормализация универсальна.

### Q: Как ускорить обработку больших документов?

**A:** 
- Используйте более мощное оборудование
- Оптимизируйте алгоритм сравнения
- Обрабатывайте документы частями

---

## Лицензия и авторство

Проект создан для сравнения DOCX документов с детальным анализом изменений.

---

## Поддержка

При возникновении проблем:
1. Проверьте версии зависимостей
2. Убедитесь, что документы не повреждены
3. Проверьте логи ошибок
4. Создайте issue с описанием проблемы

---

*Документация обновлена: 2024*

