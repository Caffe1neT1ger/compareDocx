# Сравнение DOCX документов

Проект для сравнения двух DOCX файлов с разбивкой на сегменты, разделы, главы и абзацы. Результаты сравнения экспортируются в Excel файл.

**📖 [Полная документация](docs/DOCUMENTATION.md)** - подробное описание всех возможностей, алгоритмов и примеров использования.

## Возможности

- Парсинг структуры DOCX документов (разделы, главы, абзацы)
- **Сравнение по содержимому** - сравнение документов по тексту, игнорируя стили форматирования
- Определение статуса каждого абзаца (идентичен, изменен, добавлен, удален)
- **Определение типов исправлений** - автоматическая классификация изменений (общие правки, изменение содержания, добавление/удаление текста и др.)
- **Определение подтипов изменений** - детальная классификация для каждого типа (версия документа, орфография, пунктуация и др.)
- Вычисление схожести текстов с нормализацией
- Работа с кастомными стилями (не только стандартные стили Word)
- Экспорт результатов в **Excel, JSON, CSV, HTML** с цветовой индикацией
- Статистика сравнения с детализацией по типам изменений
- Сравнение таблиц и изображений
- **Цветные прогресс-бары** для визуализации процесса обработки
- **Дополнительный анализ изменений через OpenAI LLM** (опционально) - интеллектуальный анализ изменений в деловом стиле

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. (Опционально) Для использования LLM анализа настройте конфигурацию:

   **Вариант 1: Использование .env файла (рекомендуется)**
   ```bash
   # Скопируйте пример конфигурации
   # Windows
   copy env.example .env
   
   # Linux/Mac
   cp env.example .env
   
   # Отредактируйте .env файл и укажите свой API ключ
   ```
   
   **Вариант 2: Переменные окружения**
   ```bash
   # Windows
   set OPENAI_API_KEY=your-api-key-here
   
   # Linux/Mac
   export OPENAI_API_KEY=your-api-key-here
   ```

**Примечание:** LLM анализ является опциональным. Если API ключ не задан, программа будет работать без LLM анализа.

## Использование

### Новый CLI с опциями (рекомендуется)

```bash
# Базовое сравнение с экспортом в Excel
python cli.py файл1.docx файл2.docx --xlsx

# Экспорт в несколько форматов (можно использовать флаги или --format)
python cli.py файл1.docx файл2.docx --xlsx --csv --json --html

# Или используя опцию --format
python cli.py файл1.docx файл2.docx --format excel json html

# Фильтрация только изменений
python cli.py файл1.docx файл2.docx --filter-status modified added

# Фильтрация по минимальной схожести
python cli.py файл1.docx файл2.docx --filter-min-similarity 0.8

# Отключение LLM анализа
python cli.py файл1.docx файл2.docx --no-llm

# Указание директории для результатов
python cli.py файл1.docx файл2.docx --output-dir результаты/

# Просмотр всех опций
python cli.py --help
```

**Примечание:** Результаты сохраняются в папку с временной меткой в формате `comparison_файл1_vs_файл2_YYYY-MM-DD_HH-MM-SS` для удобной организации.

### Старый способ (main.py)

```bash
# Командная строка
python main.py файл1.docx файл2.docx результат.xlsx

# Интерактивный режим
python main.py
```

Затем введите пути к файлам при запросе.

## Структура проекта

- `docx_file.py` - Класс `DocxFile` для работы с DOCX файлами
- `compare.py` - Класс `Compare` для сравнения документов
- `excel_export.py` - Класс `ExcelExporter` для экспорта в Excel
- `json_export.py` - Класс `JSONExporter` для экспорта в JSON
- `csv_export.py` - Класс `CSVExporter` для экспорта в CSV
- `html_export.py` - Класс `HTMLExporter` для экспорта в HTML
- `cli.py` - Командная строка с расширенными опциями
- `main.py` - Основной файл для запуска приложения (старый интерфейс)
- `tests/` - Папка с тестами и скриптами для создания тестовых документов
- `docs/` - Папка с документацией проекта
- `prompts/` - Папка с шаблонами промптов для LLM
- `requirements.txt` - Зависимости проекта
- `documents/` - Папка с тестовыми DOCX документами
- `results/` - Папка для сохранения результатов сравнения (с подпапками по времени)

## Тестовые документы

В папке `documents/` находятся готовые тестовые документы для проверки функционала:

- `test_document_1.docx` - Базовый документ "Техническое задание"
- `test_document_2.docx` - Модифицированная версия первого документа
- `test_document_3.docx` - Совсем другой документ "Отчет о работе"

### Пример тестирования

```bash
# Сравнение похожих документов
python main.py documents/test_document_1.docx documents/test_document_2.docx results/comparison_1_2.xlsx

# Сравнение разных документов
python main.py documents/test_document_1.docx documents/test_document_3.docx results/comparison_1_3.xlsx
```

### Создание новых тестовых документов

Для генерации тестовых документов выполните:
```bash
cd tests
python create_test_documents.py
```

Или из корня проекта:
```bash
python tests/create_test_documents.py
```

## Форматы экспорта результатов

### Excel (по умолчанию)
Результаты сохраняются в Excel файл с несколькими листами:

1. **Сравнение** - Детальное сравнение каждого абзаца:
   - Статус (идентичен/изменен/добавлен/удален)
   - Тип исправления (Общие правки, Изменение содержания, Добавление текста и др.)
   - **Подтип изменений** (версия документа, орфография, пунктуация, количество листов и др.)
   - Информация о разделе и главе
   - Тексты абзацев из обоих документов
   - Процент схожести
   - Список различий
   - Описание изменений
   - **Ответ LLM** (если LLM анализ включен)

2. **Только изменения** - Фильтрованный список только измененных элементов

3. **Статистика** - Общая статистика сравнения:
   - Количество идентичных, измененных, добавленных и удаленных абзацев
   - Процентное соотношение
   - **Статистика по типам изменений** (детальная разбивка)
   - Статистика по таблицам и изображениям
   - Количество элементов, проанализированных через LLM

4. **Краткое описание** - Краткое смысловое описание всех изменений в документе:
   - Генерируется автоматически через LLM на основе всех обнаруженных изменений
   - Включает номера страниц для каждого изменения
   - Формат: нумерованный список с группировкой похожих изменений
   - Отображается в одной ячейке для удобного чтения

5. **Таблицы** - Детальные изменения в таблицах

6. **Изображения** - Детальные изменения в изображениях

### JSON
Экспорт в структурированный JSON формат для программной обработки:
```bash
python cli.py файл1.docx файл2.docx --format json -o результат.json
```

### CSV
Экспорт в CSV файлы (создается несколько файлов для разных типов данных):
```bash
python cli.py файл1.docx файл2.docx --format csv --output-dir результаты/
```

### HTML
Интерактивный HTML отчет с фильтрацией и поиском:
```bash
python cli.py файл1.docx файл2.docx --format html -o отчет.html
```

## Цветовая индикация в Excel

- 🟢 Зеленый - Идентичные абзацы
- 🟡 Желтый - Измененные абзацы
- 🔴 Красный - Добавленные/удаленные абзацы

## Сравнение по содержимому (игнорирование стилей)

**Важная особенность:** Программа сравнивает документы **по содержимому текста**, а не по стилям форматирования.

Это означает:
- ✅ Абзацы с одинаковым текстом, но разными стилями → **Идентичны**
- ✅ Абзацы с одинаковым текстом, но разным форматированием (жирный/курсив) → **Идентичны**
- ✅ Абзацы с одинаковым текстом, но разными пробелами → **Идентичны** (после нормализации)
- ❌ Абзацы с разным текстом → **Изменены** (даже если стили одинаковые)

### Как это работает:

1. **Нормализация текста** - удаление лишних пробелов, нормализация переносов строк
2. **Сравнение по содержимому** - абзацы сопоставляются по нормализованному тексту
3. **Игнорирование стилей** - стили форматирования не влияют на результат сравнения

**Пример:**
- Документ 1: `[Стиль: "Заголовок 1"] Настоящее техническое задание`
- Документ 2: `[Стиль: "Обычный"] Настоящее техническое задание`
- **Результат:** Идентичны (100% схожесть), в описании: "изменено только форматирование"

Подробнее см. файл [docs/CONTENT_BASED_COMPARISON.md](docs/CONTENT_BASED_COMPARISON.md)

## Поддержка кастомных стилей

Программа поддерживает работу с кастомными стилями Word, а не только со стандартными стилями "Heading 1", "Heading 2" и т.д.

### Как это работает:

1. **Outline Level** (приоритет) - программа проверяет уровень структуры документа, заданный в стиле
2. **Паттерны в названиях** - распознаются стили с названиями типа "Заголовок 1", "H1", "Title" и т.д.
3. **Анализ форматирования** - если стиль не распознан, анализируется размер шрифта, жирность, длина текста
4. **Нумерация** - распознаются нумерованные заголовки ("1.1.", "1.1.1." и т.д.)

### Рекомендации:

Для лучшей работы с кастомными стилями рекомендуется:
- Установить **Outline Level** (уровень структуры) для стилей заголовков в Word
- Использовать понятные названия стилей (например, "Заголовок_Раздела", "Заголовок_Подраздела")

Подробнее см. файл [docs/CUSTOM_STYLES_SUPPORT.md](docs/CUSTOM_STYLES_SUPPORT.md)

## Дополнительный анализ через LLM

Программа поддерживает дополнительный анализ изменений через OpenAI LLM для получения более детальных и профессиональных описаний изменений в деловом стиле.

### Как это работает:

1. **Автоматическая инициализация** - при запуске программа проверяет наличие API ключа OpenAI
2. **Анализ изменений** - для каждого измененного, добавленного или удаленного элемента отправляется запрос к LLM
3. **Деловой стиль ответов** - LLM использует специальный системный промпт для генерации ответов в деловом стиле
4. **Результаты в Excel** - ответы LLM сохраняются в отдельном столбце "Ответ LLM"

### Настройка:

1. Получите API ключ OpenAI на [platform.openai.com](https://platform.openai.com)
2. Создайте файл `.env` на основе `env.example`:
   ```bash
   # Windows
   copy env.example .env
   
   # Linux/Mac
   cp env.example .env
   ```
3. Откройте файл `.env` и укажите свой API ключ:
   ```
   OPENAI_API_KEY=sk-your-api-key-here
   ```
4. (Опционально) Настройте другие параметры в `.env`:
   - `OPENAI_MODEL` - модель для использования (по умолчанию: gpt-3.5-turbo)
   - `OPENAI_TEMPERATURE` - температура генерации (по умолчанию: 0.3)
   - `OPENAI_MAX_TOKENS` - максимальное количество токенов (по умолчанию: 200)
   - `OPENAI_API_URL` - кастомный URL API (если используете прокси или другой провайдер)
5. Запустите программу - LLM анализ будет выполнен автоматически

### Пример ответа LLM:

- **Без LLM:** "Пункт 2.2. абзац 2, страница 23. '2.0.3' изменено на '2.0.4'"
- **С LLM:** "Обновлена версия системы с 2.0.3 на 2.0.4 в описании технических характеристик. Изменение внесено в пункт 2.2 на странице 23."

### Настройка модели:

По умолчанию используется модель `gpt-3.5-turbo`. Для изменения модели отредактируйте файл `.env`:

```env
OPENAI_MODEL=gpt-4
OPENAI_TEMPERATURE=0.3
OPENAI_MAX_TOKENS=200
```

### Настройка промптов:

Промпты для LLM хранятся в папке `prompts/`:
- `system_prompt.txt` - системный промпт (роль и стиль ответов)
- `user_prompt_template.txt` - шаблон пользовательского промпта

Вы можете редактировать эти файлы для изменения стиля и формата ответов LLM.

## Документация

Вся документация находится в папке [`docs/`](docs/):

- **[DOCUMENTATION.md](docs/DOCUMENTATION.md)** - Полная документация проекта
- **[CUSTOM_STYLES_SUPPORT.md](docs/CUSTOM_STYLES_SUPPORT.md)** - Поддержка кастомных стилей
- **[CONTENT_BASED_COMPARISON.md](docs/CONTENT_BASED_COMPARISON.md)** - Сравнение по содержимому

## Структура кода

Проект разделен на модули:

- **docx_file.py** - Парсинг DOCX документов (разделы, главы, таблицы, изображения)
- **compare.py** - Логика сравнения документов (нормализация, сопоставление, анализ изменений)
- **excel_export.py** - Экспорт результатов в Excel (форматирование, цветовая индикация)
- **llm_adapter.py** - Адаптер для работы с OpenAI LLM (дополнительный анализ изменений)
- **main.py** - Точка входа в программу (командная строка и интерактивный режим)
- **config.py** - Конфигурация проекта (настройки сравнения, документов, LLM, экспорта)
- **logger_config.py** - Настройка логирования для отладки и мониторинга
- **exceptions.py** - Кастомные исключения для лучшей обработки ошибок
- **validators.py** - Валидация входных данных (пути, размеры файлов, структура)
- **prompts/** - Папка с шаблонами промптов для LLM
- **env.example** - Пример файла конфигурации для LLM

Все модули содержат подробные комментарии и docstrings.

## Улучшения проекта

Проект был улучшен для повышения качества кода, производительности и функциональности:

- ✅ **Централизованная конфигурация** - все настройки в одном месте (`config.py`)
- ✅ **Система логирования** - подробное логирование для отладки (`logger_config.py`)
- ✅ **Кастомные исключения** - информативные сообщения об ошибках (`exceptions.py`)
- ✅ **Валидация входных данных** - проверка файлов и путей (`validators.py`)
- ✅ **Оптимизация производительности** - улучшенный алгоритм расчета страниц
- ✅ **Безопасность** - ограничения на размер файлов и структуру документов
- ✅ **Типы и подтипы исправлений** - автоматическая классификация изменений
- ✅ **Множественные форматы экспорта** - Excel, JSON, CSV, HTML
- ✅ **Цветные прогресс-бары** - визуализация процесса обработки
- ✅ **Комплексное тестирование** - 61 тест с покрытием кода 67%
- ✅ **CLI с расширенными опциями** - фильтрация, множественные форматы, настройка логирования

Подробнее см. [docs/IMPROVEMENTS.md](docs/IMPROVEMENTS.md)

### Варианты дальнейших улучшений

Список возможных улучшений проекта с описанием, приоритетом и сложностью реализации доступен в [docs/IMPROVEMENT_OPTIONS.md](docs/IMPROVEMENT_OPTIONS.md).

**Уже реализовано:**
- ✅ Экспорт в JSON/HTML/CSV
- ✅ Фильтрация результатов
- ✅ Прогресс-бары
- ✅ Retry логика и таймауты для LLM
- ✅ Unit-тесты и интеграционные тесты
- ✅ CLI с опциями

**Возможные направления:**
- 🚀 Производительность (распараллеливание LLM, кэширование, оптимизация памяти)
- 📊 Функциональность (дополнительные форматы экспорта, расширенная фильтрация)
- 🛡️ Надежность (дополнительная валидация, обработка ошибок)
- 🧪 Тестирование (увеличение покрытия кода, тесты производительности)
- 🔧 Удобство использования (веб-интерфейс, графическое отображение)
- 🌐 API и интеграция (REST API, Python API)

